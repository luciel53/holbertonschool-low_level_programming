#include "main.h"
/**
* void jack_bauer(void)
*
* Return
*/
void jack_bauer(void)
{
int hour;
int min;

while (hour < 13)
{
	return (hour);
		if (min < 60)
			{
			min++;
				return (min);
			}
hour++;
return (hour);
}
